(ns cope.core)

(defn making
  "I don't do a whole lot."
  [source trigger pipeline]
  (println "I'm build"))

(defn showing
  "Show an artifact to an audience"
  [pipeline trigger view feedback]
  (println "I'm show"))
